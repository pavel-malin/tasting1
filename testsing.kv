<FloatingButton@MDFloatingActionButton>:
    x: Window.width - (self.width + dp(21))
    y: dp(25)
    size_hint: None, None
    size: dp(46), dp(46)
    elevation: 5
    md_bg_color: app.theme_cls.primary_color
    on_release: self.parent.callback(self)

    <FloatingLabel>:
    size_hint: None, None
    height: dp(20)
    width: label.texture_size[0]
    border_color_a: .5
    md_bg_color: app.theme_cls.primary_color
    x: -self.width

    Label:
        id: Label:
        color: 0, 0, 0, 1
        bold: True
        markup: True
        text: '  %s  ' % root.text

<FloatingActionButton>:
    FloatingButton:
        id: f_btn_1
        icon: list(root.floating_data.value())[0]
    FloatingButton:
        id: f_btn_2
        icon: list(root.floating_data.value())[1]
    FloatingButton:
        id: f_btn_3
        icon: list(root.floating_data.value())[2]
    FloatingLabel:
        id: f_lbl_1
        text: list(root.floating_data.keys())[0]
    FloatingLabel:
        id: f_lbl_2
        text: list(root.floating_data.keys())[1]
    FloatingLabel:
        id: f_lbl_3
        text: list(root.floating_data.keys())[2]
    MDFloatingActionButton:
        icon: root.icon
        size: dp(56), dp(56)
        x: Window.width - (self.width + dp(15))
        md_bg_color: app.theme_cls.primary_color
        y: dp(15)
        on_release: root.show_floating_buttons()